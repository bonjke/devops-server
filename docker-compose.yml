version: '3.8'

services:
  devops-center:
    build: .
    container_name: devops-center
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./tasks:/app/tasks
      - ./results:/app/results
      - ./logs:/app/logs
      - ./src:/app/src
      # Docker socket
      - /var/run/docker.sock:/var/run/docker.sock
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - devops-net
    environment:
      - DOCKER_HOST=unix:///var/run/docker.sock
    # Logging configuration for automated log reading
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  devops-net:
    driver: bridge
